defaults:
  - CL14_cosmic_joint_shadow_sa128_softmask_24k
  - _self_

# Shared CL15-CL20 contract. Child configs change one mechanism or dataset.
pipeline:
  pose_adapt_ratio: 0.0
  ca_mixing_for_face: false

trainer:
  epoch_len: 2000
  n_epochs: 12
  validation_interval_steps: 2000

writer:
  loss_names: [loss, loss_ba_aux, loss_ba_ownership, loss_ba_crossview]

