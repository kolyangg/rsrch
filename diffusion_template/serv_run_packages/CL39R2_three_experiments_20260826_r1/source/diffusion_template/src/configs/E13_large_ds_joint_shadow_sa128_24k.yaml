defaults:
  - large_dataset_joint_r128_24k
  - _self_

validation_shadow_photomaker_default: true

writer:
  experiment_comment: >-
    E13 historical-mechanism reconstruction: jointly train hard spatial BA
    rank128, the effective generic rank32 adapter, and effective PhotoMaker
    default rank64 at 1e-4; schema-v2 checkpoints save all paths while full-96
    validation restores the pretrained PhotoMaker default to measure the
    historical shadow-coadapter mechanism.
