# Retained artifacts

`reference_debug/april_attention_masks/` contains deliberately retained visual
debugging examples from the historical attention-mask workflow.
New runtime outputs, checkpoints, and logs should be written to ignored output
directories rather than committed here.
