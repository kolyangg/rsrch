# Architecture cards

Every runnable ID in this folder maps directly to a key in
`../architecture_registry.json`. The registry is the executable source of
truth; these cards record the hypothesis, exact scope, and interpretation.

All cards preserve NN3a_new1's core branched-attention logic. Variants change
only optimizer scope, projection learning rates, or training loss unless a
card explicitly states otherwise.
