# Archive

Historical entry points and integration examples are retained here for
provenance. They are not part of the supported April RHCA execution path.

Some apparently old directories inside `src/` remain in place because active
modules still import them; do not merge those into this archive without a
separate code audit.
