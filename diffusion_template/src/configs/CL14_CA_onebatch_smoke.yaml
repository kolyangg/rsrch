defaults:
  - CL14_CA
  - _self_

# Operational smoke only: one unchanged CL14 validation batch (batch size 12)
# before entering training.
datasets:
  val:
    manual_val:
      limit: 12

trainer:
  face_quality:
    expected_images: 12

writer:
  experiment_comment: >-
    Operational CL14_CA startup smoke with one 12-item manual_val batch. This
    run is not scientifically comparable; stop after finite optimizer steps.
