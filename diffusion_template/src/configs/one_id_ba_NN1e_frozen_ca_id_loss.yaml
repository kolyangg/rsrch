defaults:
  - one_id_ba_NN1d_frozen_ca
  - _self_

# NN1d plus low-noise decoded identity supervision against the reference image.
model:
  use_id_loss: true
  id_loss_weight: 0.1
  id_loss_max_timestep: 400
  id_loss_face_size: 160
  id_loss_identity_source: reference

