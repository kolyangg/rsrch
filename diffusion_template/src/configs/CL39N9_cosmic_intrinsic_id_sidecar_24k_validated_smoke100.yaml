defaults: [CL39N9_cosmic_intrinsic_id_sidecar_24k, _self_]

trainer:
  epoch_len: 100
  n_epochs: 1
  validation_interval_steps: null
  skip_initial_validation: false

writer:
  experiment_comment: >-
    Sealed fixed-96 step-zero lifecycle plus 100-step throughput qualification
    for CL39N9; scientific settings are inherited unchanged.
