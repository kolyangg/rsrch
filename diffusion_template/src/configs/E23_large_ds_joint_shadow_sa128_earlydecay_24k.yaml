defaults:
  - E13_large_ds_joint_shadow_sa128_24k
  - _self_

lr_scheduler:
  hold_steps: 8000

writer:
  experiment_comment: >-
    E23 vs E13 changes only WarmupHoldCosineLR hold_steps from 14,000 to 8,000.
    Warmup, total steps, minimum factor, all three base LRs, architecture,
    loss, data, and shadow validation remain unchanged.

