defaults:
  - large_dataset_rhca_hard_v1_audited_20k
  - _self_

# E1: the only scientific delta is true normalization over valid reference keys.
model:
  ba_hard_v1_true_reference_key_mask: true

writer:
  experiment_comment: >-
    E1 vs audited Large Dataset r4 baseline: exclude non-face reference keys
    from hard-BA self-attention softmax; test whether zero-key dilution causes
    the early ID-sim plateau.
