defaults:
  - E14_large_ds_joint_shadow_sa128_protected_24k
  - _self_

validation_shadow_photomaker_default: false
ba_lr: 1e-4
generic_adapter_lr: 5e-5
photomaker_default_lr: 1e-5

writer:
  experiment_comment: >-
    E15 persistent clean candidate: E14 architecture and protected loss, but
    validate every saved path and use differential LRs (BA 1e-4, effective
    generic 5e-5, PhotoMaker default 1e-5) to retain pretrained composition.
