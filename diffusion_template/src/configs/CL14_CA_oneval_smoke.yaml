defaults:
  - CL14_CA
  - _self_

# Operational smoke only: retain CL14's initial-validation lifecycle but run
# one fixed manual_val item before entering the first training batch.
datasets:
  val:
    manual_val:
      limit: 1

writer:
  experiment_comment: >-
    Operational CL14_CA startup smoke with one initial manual_val item. This
    run is not scientifically comparable; stop after finite optimizer steps.
