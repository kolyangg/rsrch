defaults:
  - CL15_CL20_hardcase_base_24k
  - _self_

# CL16: once-per-reference clean t=1/null-text feature memory.
model:
  ba_hardcase_mode: clean_memory
  ba_hardcase_groups: [mid_block, up_blocks.0, up_blocks.1]
  ba_hardcase_rank: 64
  ba_hardcase_gate_max: 0.20

expected_trainable_contract:
  enabled: false

writer:
  experiment_comment: >-
    CL16 vs CL14 adds detached clean multi-scale reference memory with explicit
    target-Q/reference-memory-KV and a zero-initialized bounded RMS residual.

