defaults:
  - one_id_ba_NN1d_frozen_ca
  - _self_

# Reference BA owns an inner elliptical face core; target attention owns the
# surrounding face/jaw/hair/neck boundary ring.
model:
  ba_sa_ref_token_mode: full_grid
  ba_sa_face_mode: core_ring
  ba_sa_ref_layer_scope: all
  ba_sa_core_ratio: 0.7
