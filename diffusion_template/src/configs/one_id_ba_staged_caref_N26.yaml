defaults:
  - one_id_ba_staged_legacy_N25
  - _self_

# N26: N25 plus a low-LR reference-only CA probe. The CA noise path remains frozen.
train_branched_ca_lora: true
ba_ca_lr_scale: 0.1

model:
  ba_ca_train_mode: ref_only

