defaults:
  - one_id_ba_NN1d_frozen_ca
  - _self_

# Separate target and reference attention softmaxes, then learn a bounded
# per-layer/per-head blend initialized to 25% reference.
model:
  ba_sa_ref_token_mode: full_grid
  ba_sa_face_mode: dual
  ba_sa_ref_layer_scope: all
  ba_sa_mix_init: 0.25
